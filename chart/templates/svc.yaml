apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName }}
  namespace: default
spec:
  ports:
    - protocol: TCP
      name: web
      port: 8080
  selector:
    app: {{ .Values.appName }}
