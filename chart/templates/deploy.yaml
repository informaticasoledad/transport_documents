kind: Deployment
apiVersion: apps/v1
metadata:
  namespace: default
  name: {{ .Values.appName }}
  labels:
    app: {{ .Values.appName }}

spec:
  replicas: {{ .Values.replicaCount }}
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: {{ .Values.appName }}
  template:
    metadata:
      labels:
        app: {{ .Values.appName }}
    spec:
      containers:
        - name: {{ .Values.appName }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: TZ
              value: Europe/Madrid
            - name: ERPAUTH_MASTER_KEY
              value: 2KtlatXUbHPOvbrP0Pin3HfmFc35QAy4OLX6/IUgm6A=
          ports:
            - name: web
              containerPort: 8080
          resources:
{{ toYaml .Values.resources | nindent 12 }}
          livenessProbe: 
            httpGet: 
              path: /index.html 
              port: 8080
            initialDelaySeconds: 5 #Default 0 
            periodSeconds: 30 #Default 10 
            timeoutSeconds: 1 #Default 1 
            successThreshold: 1 #Default 1 
            failureThreshold: 3 #Default 3           
      imagePullSecrets:
{{ toYaml .Values.imagePullSecrets | nindent 8 }}
